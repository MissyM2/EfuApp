<div class="input group page-subtitle">
    <input type="text"
            class="form-control page-subtitle"
            placeholder="Type course name here to search"
            @bind-value="courseNameToSearch" />
    <br />
    <button type="button"
            class="btn-success input-group-text page-subtitle"
            @onclick="OnSearch">Search</button>
</div>

@code {
    private string courseNameToSearch = string.Empty;

    [Parameter]
    public EventCallback<string> OnSearchCourseList { get; set; }

    public void OnSearch()
    {
        OnSearchCourseList.InvokeAsync(courseNameToSearch);
    }

}
